language: python
python:
  - "3.6"
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  -  - chmod +x uber4movers/flask-sqlalchemy/test.py # or py.test for Python versions 3.5 and below
