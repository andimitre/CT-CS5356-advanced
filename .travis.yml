language: python
python:
  - "3.6"
sudo: required
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - sudo uber4movers/flask-sqlalchemy/test.py # or py.test for Python versions 3.5 and below
